actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
            url: http://amadeus-nginx:80
            depends: PhpBrowser
        - Asserts
        - JsonSchema
    config:
        PhpBrowser:
            url: ''
            curl:
                CURLOPT_TIMEOUT: 300
